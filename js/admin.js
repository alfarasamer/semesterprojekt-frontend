

$.getJSON('http://localhost:8080/username', function (data) {
    
/*$.each(data,function (index, category) {
        var id = category.id;
        var categoryName = category.categoryName;

        $('.output').append('<tr><th class="category-id" scope="row">'
        +id+'</th><td class="category-name">'+categoryName+'</td><td>Edit - Delete</td></tr>')

        
    });*/
    console.log(data)
});


